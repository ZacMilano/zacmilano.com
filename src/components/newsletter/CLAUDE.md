# Newsletter

- All article content will be human-written; lorem ipsum is placeholder only
- Each article should be ~1-2 desktop screen-heights of text (roughly 400-800 words)
- Images are placeholders for now — gray boxes, floated with generous margin
- Annotations are global across the newsletter, defined inline at first use via `<Annotated id="..." content={...}>`
- Annotation IDs can be reused across articles — the first registration wins
